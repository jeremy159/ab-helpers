name: ab_helpers
services:
  daemon:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      RUST_LOG: abh=debug
    volumes:
      - data:/data

  # Uncomment when deploying the API server:
  # api:
  #   build: .
  #   command: abh serve
  #   ports:
  #     - "8000:8000"
  #   environment:
  #     ABH_ENVIRONMENT: "staging"
  #   depends_on:
  #     - postgres
  #     - redis

  # postgres:
  #   image: postgres:16
  #   restart: always
  #   volumes:
  #     - db:/var/lib/postgresql/data
  #   environment:
  #     POSTGRES_PASSWORD: "password"
  #     POSTGRES_USER: "postgres"
  #     POSTGRES_DB: ab_helpers

  # redis:
  #   image: "redis:7"
  #   restart: always
  #   volumes:
  #     - redis:/data

volumes:
  data:
  # db:
  # redis:
  #   driver: local